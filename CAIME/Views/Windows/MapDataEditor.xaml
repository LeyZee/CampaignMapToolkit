<Window x:Class="CAIME.Windows.MapDataEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CAIME.Views"
        mc:Ignorable="d"
        Title="Map Data Editor"
        MinHeight="600"
        MinWidth="300"
        MaxWidth="700"
        MaxHeight="900"
        WindowStyle="ThreeDBorderWindow"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundDarkV1Brush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Templates/MapDataRegionTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Settings -->
            <RowDefinition Height="*"/>     <!-- List of regions -->
            <RowDefinition Height="Auto"/>  <!-- Actions -->
        </Grid.RowDefinitions>

        <local:MapDataSettings Grid.Column="0"
                               Grid.Row="0"
                               HorizontalAlignment="Stretch"
                               x:Name="settings"/>

        <ListBox Grid.Column="0"
                 Grid.Row="1"
                 Background="{StaticResource BackgroundDarkV0Brush}"
                 ItemTemplate="{StaticResource MapDataRegionTemplate}"
                 ItemsSource="{Binding MapDataRegions}">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Margin="5"
                                            Orientation="Vertical"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

        </ListBox>

        <Button Grid.Column="0"
                Grid.Row="2"
                Content="Save changes"
                Width="80"
                Margin="3"
                HorizontalAlignment="Right"
                Style="{StaticResource DarkButton}"
                Click="SaveChanges_Click"/>

    </Grid>
    
</Window>
